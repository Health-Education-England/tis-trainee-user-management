application:
  aws:
    cognito:
      user-pool-id: eu-west-2_dummy
      beta-participant-group: test_group
    sqs:
        cognito-events: tis-trainee-user-management-local-cognito-events
        contact-details:
            updated: tis-trainee-user-management-local-contact-details-updated

spring:
  cloud:
    aws:
      region:
        static: aws-global
      sns:
        enabled: false
      sqs:
        enabled: false

